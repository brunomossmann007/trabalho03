name: Trigger auto deployment for container-app-cpjz3x74elu7a

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '**'
    - '.github/workflows/container-app-cpjz3x74elu7a-AutoDeployTrigger-fa9d08a5-e85b-4126-bb7d-366699de92f2.yml'

  # Allow mannually trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.CONTAINERAPPCPJZ3X74ELU7A_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CONTAINERAPPCPJZ3X74ELU7A_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CONTAINERAPPCPJZ3X74ELU7A_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v1
        with:
          appSourcePath: ${{ github.workspace }} 
          registryUrl: containerregistrycpjz3x74elu7a.azurecr.io
          registryUsername: ${{ secrets.CONTAINERAPPCPJZ3X74ELU7A_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CONTAINERAPPCPJZ3X74ELU7A_REGISTRY_PASSWORD }}
          containerAppName: container-app-cpjz3x74elu7a
          resourceGroup: trabalho
          imageToBuild: containerregistrycpjz3x74elu7a.azurecr.io/container-app-cpjz3x74elu7a:${{ github.sha }}
          dockerfilePath: Dockerfile


